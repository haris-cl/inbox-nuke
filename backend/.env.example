# =============================================================================
# Google OAuth 2.0 Credentials
# =============================================================================
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# See docs/OAUTH_SETUP.md for detailed setup instructions

# Your Google OAuth 2.0 Client ID (format: 123456789-abc123.apps.googleusercontent.com)
GOOGLE_CLIENT_ID=your-client-id-here

# Your Google OAuth 2.0 Client Secret (format: GOCSPX-xxxxxxxxxx)
GOOGLE_CLIENT_SECRET=your-client-secret-here

# OAuth callback URL - must match exactly in Google Cloud Console
# For local development, use: http://localhost:8000/api/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback

# =============================================================================
# Encryption Key
# =============================================================================
# Generate a Fernet encryption key for securely storing OAuth tokens in the database
# Run this command to generate a key:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-generated-fernet-key-here

# =============================================================================
# Application Environment
# =============================================================================
# Options: local, production
APP_ENV=local

# =============================================================================
# Frontend URL (for CORS)
# =============================================================================
# The URL where your Next.js frontend is running
# For local development: http://localhost:3000
FRONTEND_URL=http://localhost:3000

# =============================================================================
# OpenAI API Key (Optional)
# =============================================================================
# Only needed if you want AI-powered email classification features
# Get your API key from: https://platform.openai.com/api-keys
# Leave blank or comment out if not using AI features
# OPENAI_API_KEY=sk-proj-your-key-here
